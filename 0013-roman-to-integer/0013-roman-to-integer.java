class Solution {
    public int romanToInt(String s) {
        for(int i=s.length()-1;i>=0;i++){
            int current=switch(s.charAt(i)){
                case 'I'->1;
                case 'I'->1;
            };
        }
    }
}